<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin Dashboard - Casmitter" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet"/>
    <link rel="icon" href="<%= asset_path('favicon.ico') %>" type="image/x-icon">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b border-gray-200">
      <div class="flex items-center justify-between px-6 py-4">
        <div class="flex items-center space-x-4">
          <img alt="Casmitter logo" class="h-10 w-10 rounded-full" src="<%= asset_path("teahour2-logo.jpg")%>"/>
          <h1 class="text-xl font-semibold text-gray-900">Admin Dashboard</h1>
        </div>
        
        <div class="flex items-center space-x-4">
          <span class="text-sm text-gray-600">
            Welcome, <%= Current.user&.email_address || Current.user&.email_address || "Admin" %>
          </span>
          <%= link_to "Logout", session_path, method: :delete, 
              class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
        </div>
      </div>
    </header>

    <div class="flex h-screen bg-gray-50">
      <!-- Sidebar -->
      <aside class="w-64 bg-white shadow-sm border-r border-gray-200">
        <nav class="mt-8">
          <div class="px-4 py-2">
            <h3 class="text-xs font-semibold text-gray-500 uppercase tracking-wider">Main Navigation</h3>
          </div>
          
          <div class="mt-4">
            <div class="flex items-center px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gray-50 hover:text-gray-900 border-r-2 border-transparent hover:border-blue-500 transition-colors duration-200">
              <i class="fas fa-tachometer-alt w-5 h-5 mr-3"></i>
              Dashboard
            </div>
            
            <%= link_to admin_episodes_path, class: "flex items-center px-4 py-3 text-sm font-medium text-gray-700 hover:bg-gray-50 hover:text-gray-900 border-r-2 border-transparent hover:border-blue-500 transition-colors duration-200" do %>
              <i class="fas fa-podcast w-5 h-5 mr-3"></i>
              Episodes
            <% end %>
            
        </nav>
      </aside>

      <!-- Main Content -->
      <main class="flex-1 overflow-auto">
        <div class="p-6">
          <% if notice %>
            <div class="mb-4 bg-green-50 border border-green-200 text-green-800 px-4 py-3 rounded-md">
              <%= notice %>
            </div>
          <% end %>
          
          <% if alert %>
            <div class="mb-4 bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-md">
              <%= alert %>
            </div>
          <% end %>
          
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
